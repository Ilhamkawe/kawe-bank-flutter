import 'package:flutter/material.dart';

class SignUpUpdateProfile extends StatelessWidget {
  const SignUpUpdateProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
